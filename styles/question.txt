用rem布局对比px有什么好处。

为什么不能t通过id或class直接获取content>cont_intro下的p、h3等元素。

电视tvclose_up,tvclose_down不能隐藏的原因是没有把tvclose的overflow属性设置为hidden，

点击电视关闭按钮之所以会移动是因为滚动条的原因，解决办法是给body或最外一层的div（fisrtPage）的overflow属性设置为hidden。

fileleft之后的蓝色图片会被张臻/apollo zhang这个P标签覆盖，
解决办法：在直接包裹它们的外层div的z-index做相应的调整。

找到快速书写css兼容格式的方法，不要每次都去其他地方复制粘贴到要书写的地方。